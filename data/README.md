# Data Directory

This directory contains:

- `vector_store/` - FAISS vector indices
- `files/` - User uploaded files (if enabled)
- `output/` - Generated output files (if enabled)
- `cache/` - Cached embeddings and responses

## Note

This directory is `.gitignore`d. The structure will be created automatically when needed.
